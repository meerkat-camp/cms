<% if @post.errors.present? %>
  <%= turbo_stream.replace("form", partial: "form", locals: {post: @post}) %>
<% else %>
  <%= turbo_stream_action_tag("redirect", location: site_posts_path) %>
<% end%>
