<% if @page.errors.present? %>
  <%= turbo_stream.replace 'form', partial: 'form', locals: { page: @page } %>
<% else %>
  <%= turbo_stream_action_tag("redirect", location: site_pages_path) %>
<% end %>
